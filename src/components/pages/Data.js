const aboutProps = {
    id: "about",
    isDark: false,
    isRed: false
}

const projectsProps = {
    id: "projects",
    isDark: true,
    isRed: true
}

const githubProps = {
    id: "github",
    isDark: false,
    isRed: false
}

const contactProps = {
    id: "contact",
    isDark: true,
    isRed: false
}

const homeProps = {
    id: "home",
    isDark: false,
    isRed: false
}

module.exports = {aboutProps, projectsProps, githubProps, contactProps, homeProps}