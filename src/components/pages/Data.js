const aboutProps = {
    id: "about",
    isDark: false
}

const projectsProps = {
    id: "projects",
    isDark: true
}

const githubProps = {
    id: "github",
    isDark: false
}

const contactProps = {
    id: "contact",
    isDark: true
}

const homeProps = {
    id: "home",
    isDark: false
}

module.exports = {aboutProps, projectsProps, githubProps, contactProps, homeProps}